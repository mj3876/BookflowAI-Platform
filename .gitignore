# ─── Claude Code 개인 설정 (public repo 방지) ───
.claude/
.mcp.json
CLAUDE.md

# ─── Cross-cloud exports (매일 새로 생성 · 팀 단톡 공유용) ───
exports/

# ─── 임시 파일 (스크립트가 render한 PNG 등 실수 방지) ───
_tmp_*
*.tmp
tmp/

# ─── Local-only 임시 검증 task (일회성 · git push 안 함) ───
scripts/aws/tasks/cicd_verify.py

# ─── 개인별 설정 override ───
scripts/aws/config/aws.local.ps1
scripts/azure/config/azure.local.ps1
scripts/gcp/config/gcp.local.ps1

# ─── 환경 변수 / 시크릿 ───
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
*-credentials.json

# ─── AWS / 클라우드 로컬 state ───
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfplan
terraform.tfvars
!terraform.tfvars.example

# AWS SAM
.aws-sam/
samconfig.toml

# Azure Bicep
main.json
*.bicepbuild

# ─── IDE / OS ───
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini

# ─── Python ───
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# ─── Node ───
node_modules/
npm-debug.log*
yarn-debug.log*
.npm

# ─── Ansible ───
*.retry
ansible.log

# ─── 로컬 빌드 산출물 ───
dist/
build/
*.zip
*.tar.gz

# ─── 기타 ───
.coverage
htmlcov/
*.log

# Terraform security / local artifacts
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
*.tfbackup
*.tfplan.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info
terraform.log
terraform-provider-*.log

# Temporary PDF processing images
tmp_pdf_images/

scripts/gcp/data/
# Local-only generated CSVs (regenerated from BOOKFLOW/01_데이터스키마/*.parquet)
infra/aws/20-data-persistent/seed-data/parquet_csv/
