# Local credentials and exported service-account keys must never be committed.
.terraform/
terraform.tfstate
terraform.tfstate.backup
*.tfplan

sa-key.json
*-sa-key.json
*service-account*.json
*credentials*.json
